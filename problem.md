## **問題5：無限ループ防止**

フォーム入力に応じて、入力値の長さが変更されるたびにコンソールに「入力文字数: [length]」と表示するコンポーネントを作成せよ。ただし、無限ループを避けるために依存配列を正しく設定すること。

---

### **条件**

(1) `inputValue` を `useState` で管理すること
(2) `useEffect` の依存配列に `inputValue` を設定すること
(3) TailwindCSSで入力フィールドを枠線付きにすること